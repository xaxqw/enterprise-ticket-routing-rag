[pytest]
# 测试目录与导入根：pythonpath=. 让 `import app` 无需手动 sys.path 注入
testpaths = tests
pythonpath = .
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
