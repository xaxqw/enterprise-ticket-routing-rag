# ========================================================================
# 多智能体 RAG 问答平台 —— 运行时依赖（后端 API / Celery / 前端）
# 说明：本文件只包含线上运行所需依赖，不含 LoRA 训练（见 requirements-finetune.txt）
# 安装：pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# ========================================================================

# ---- Web 框架 / 服务 ----
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9          # UploadFile 文件上传依赖
pydantic==2.9.2

# ---- 配置 / 环境变量 ----
python-dotenv==1.0.1

# ---- 缓存 / 消息队列 ----
redis==5.0.8
celery==5.4.0

# ---- 大模型 / Embedding / Rerank（硅基流动 OpenAI 兼容在线 API）----
openai==1.51.0
requests==2.32.3

# ---- 向量检索 ----
faiss-cpu==1.8.0
numpy==1.26.4

# ---- 关键词检索（BM25）----
rank-bm25==0.2.2
jieba==0.42.1

# ---- 认证 / JWT ----
python-jose[cryptography]==3.3.0

# ---- 多源数据流水线：文档解析 ----
pypdf==4.3.1                     # PDF 文本解析
PyMuPDF>=1.24.0                  # PDF 页面渲染（扫描件 OCR / 以文搜图 用，无需外部 poppler）
python-docx==1.1.2               # Word 解析
openpyxl==3.1.5                  # Excel 解析
beautifulsoup4==4.12.3           # 网页/URL 数据源解析
lxml==5.3.0                      # bs4 解析后端，速度快

# ---- 前端可视化（Streamlit 问答 + 监控面板）----
streamlit==1.38.0
pandas==2.2.2
plotly==5.24.1

# ---- 自动化评测 / 测试 ----
pytest==8.3.3
scikit-learn==1.5.2              # 评测指标 / 向量去重相似度计算
httpx==0.27.2                    # FastAPI TestClient 依赖（API 层测试）
fakeredis==2.24.1                # CI / 本地用内存版 Redis 替代真实实例（测试用，非运行时依赖）

# ---- 压测（可选）----
locust==2.31.5
